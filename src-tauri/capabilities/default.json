{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default app capabilities",
  "windows": ["floating", "pick_count", "pick_result", "config"],
  "permissions": [
    "core:default"
  ]
}
